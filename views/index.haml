%link{ rel: "stylesheet", type: "text/css", href: "https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css"}
%h1 Latest Ticket Sales

- if @transactions
  %table.table.striped-table
    %thead
      %th Time
      %th First
      %th Last
      %th Email
    %tbody
      - @transactions.each do |t|
        %tr
          %td= t.created_at
          %td= t.first_name
          %td= t.last_name
          %td= t.email
- else
  %p No current transactions
